import { ConnectionPropertiesDialogDirective } from './connection-properties-dialog.directive';

describe('ConnectionPropertiesDialogDirective', () => {
  it('should create an instance', () => {
    const directive = new ConnectionPropertiesDialogDirective();
    expect(directive).toBeTruthy();
  });
});
