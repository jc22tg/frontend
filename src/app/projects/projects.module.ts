import { NgModule } from '@angular/core';

// Este módulo ya no es necesario porque todos los componentes son standalone.
// Mantenido solo por razones de compatibilidad.
@NgModule({})
export class ProjectsModule { } 
